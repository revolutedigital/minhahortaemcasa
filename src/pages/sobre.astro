---
import Layout from '../layouts/Layout.astro';
import Mascote from '../components/illustrations/Mascote.astro';
import AvatarIcons from '../components/illustrations/AvatarIcons.astro';
import PlantCardIcons from '../components/illustrations/PlantCardIcons.astro';

export const prerender = true;

const values = [
  {
    title: 'Simplicidade',
    description: 'Guias praticos e diretos, sem termos tecnicos desnecessarios.',
    icon: 'simple'
  },
  {
    title: 'Acessibilidade',
    description: 'Horta em casa e para todos, independente do espaco disponivel.',
    icon: 'access'
  },
  {
    title: 'Sustentabilidade',
    description: 'Cultivar sua propria comida e um ato de amor ao planeta.',
    icon: 'sustain'
  },
  {
    title: 'Comunidade',
    description: 'Compartilhar conhecimento e ajudar outros a crescer junto.',
    icon: 'community'
  }
];

const stats = [
  { number: '2.500+', label: 'Hortas criadas' },
  { number: '15+', label: 'Guias publicados' },
  { number: '4.9', label: 'Avaliacao media' },
  { number: '98%', label: 'Taxa de sucesso' }
];
---

<Layout
  title="Sobre Nos"
  description="Conheca a historia do Horta em Casa e nossa missao de ajudar brasileiros a cultivar sua propria comida em casa."
>
  <!-- Hero Section -->
  <section class="relative py-20 overflow-hidden">
    <!-- Background -->
    <div class="absolute inset-0 bg-gradient-to-br from-leaf-50 via-white to-earth-50"></div>
    <div class="absolute top-20 left-10 w-72 h-72 bg-leaf-200/30 rounded-full blur-3xl blob-animate"></div>
    <div class="absolute bottom-10 right-10 w-64 h-64 bg-earth-200/20 rounded-full blur-3xl blob-animate" style="animation-delay: -3s;"></div>

    <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-col lg:flex-row items-center gap-12">
        <!-- Content -->
        <div class="flex-1 text-center lg:text-left">
          <nav class="text-sm text-gray-500 mb-4">
            <a href="/" class="hover:text-leaf-600 transition-colors">Inicio</a>
            <span class="mx-2">/</span>
            <span class="text-gray-900 font-medium">Sobre</span>
          </nav>

          <h1 class="text-4xl sm:text-5xl font-bold text-gray-900 mb-6">
            Nossa <span class="text-gradient">Historia</span>
          </h1>

          <p class="text-xl text-gray-600 max-w-xl mb-8">
            Nascemos da paixao por plantas e da vontade de ajudar mais brasileiros a cultivar sua propria comida em casa.
          </p>

          <div class="flex flex-wrap gap-3 justify-center lg:justify-start">
            <span class="badge badge-leaf">Desde 2023</span>
            <span class="badge badge-earth">Feito no Brasil</span>
            <span class="badge badge-sun">Para brasileiros</span>
          </div>
        </div>

        <!-- Mascote -->
        <div class="relative">
          <Mascote mood="waving" size="xl" />

          <!-- Floating elements -->
          <div class="absolute -top-4 -right-6 animate-float bg-white rounded-2xl shadow-lg p-3">
            <PlantCardIcons type="manjericao" size="sm" />
          </div>
          <div class="absolute -bottom-2 -left-4 animate-float bg-white rounded-2xl shadow-lg p-3" style="animation-delay: -2s;">
            <PlantCardIcons type="hortela" size="sm" />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Mission Section -->
  <section class="py-16 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 scroll-reveal">
        <span class="badge badge-leaf mb-4">Nossa Missao</span>
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-6">
          Democratizar o cultivo em casa
        </h2>
      </div>

      <div class="bg-gradient-to-br from-leaf-50 to-earth-50 rounded-3xl p-8 sm:p-12 scroll-reveal">
        <div class="prose prose-lg max-w-none text-center">
          <p class="text-gray-600 text-lg leading-relaxed mb-6">
            Acreditamos que todo brasileiro pode e deveria ter acesso a temperos e ervas frescos em casa.
            Nao importa se voce mora em apartamento de 30m2 ou em casa com quintal - existe uma forma
            de cultivar sua propria comida.
          </p>
          <p class="text-gray-600 text-lg leading-relaxed mb-6">
            O <strong class="text-leaf-700">Horta em Casa</strong> nasceu quando percebemos que muitas pessoas
            queriam ter uma horta, mas nao sabiam por onde comecar. As informacoes na internet sao confusas,
            tecnicas demais ou simplesmente nao funcionam para o clima brasileiro.
          </p>
          <p class="text-gray-600 text-lg leading-relaxed">
            Por isso, criamos guias <strong class="text-leaf-700">praticos, simples e adaptados</strong> para
            a realidade de quem mora em apartamento no Brasil. Nosso objetivo e que voce colha seus primeiros
            temperos em ate 30 dias.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="py-12 bg-gray-50">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        {stats.map((stat, index) => (
          <div class={`text-center scroll-reveal reveal-delay-${index + 1}`}>
            <div class="text-3xl sm:text-4xl font-bold text-leaf-600 mb-1">{stat.number}</div>
            <div class="text-sm text-gray-600">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Verdinho Section -->
  <section class="py-16 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-col md:flex-row items-center gap-12">
        <div class="flex-shrink-0 scroll-reveal">
          <div class="relative">
            <Mascote mood="happy" size="xl" />
            <div class="absolute -bottom-2 left-1/2 -translate-x-1/2 bg-leaf-100 px-4 py-2 rounded-full">
              <span class="text-leaf-700 font-semibold text-sm">Verdinho</span>
            </div>
          </div>
        </div>

        <div class="flex-1 text-center md:text-left scroll-reveal reveal-delay-2">
          <span class="badge badge-sun mb-4">Nosso Mascote</span>
          <h2 class="text-3xl font-bold text-gray-900 mb-4">
            Conheca o Verdinho
          </h2>
          <p class="text-gray-600 mb-6">
            O Verdinho e nosso mascote e guia. Ele representa a alegria de ver suas plantas
            crescerem e a satisfacao de colher o que voce mesmo plantou.
          </p>
          <p class="text-gray-600 mb-6">
            Voce vai encontra-lo em todo o site, sempre pronto para ajudar - seja pensando
            em qual planta recomendar, animado com uma nova dica, ou ate mesmo dormindo
            na pagina 404!
          </p>
          <div class="flex flex-wrap gap-4 justify-center md:justify-start">
            <div class="flex items-center gap-2 bg-leaf-50 px-3 py-2 rounded-lg">
              <Mascote mood="happy" size="sm" />
              <span class="text-sm text-leaf-700">Feliz</span>
            </div>
            <div class="flex items-center gap-2 bg-leaf-50 px-3 py-2 rounded-lg">
              <Mascote mood="thinking" size="sm" />
              <span class="text-sm text-leaf-700">Pensativo</span>
            </div>
            <div class="flex items-center gap-2 bg-leaf-50 px-3 py-2 rounded-lg">
              <Mascote mood="excited" size="sm" />
              <span class="text-sm text-leaf-700">Animado</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Values Section -->
  <section class="py-16 bg-gradient-to-br from-leaf-50 to-earth-50">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 scroll-reveal">
        <span class="badge badge-earth mb-4">Nossos Valores</span>
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">
          O que nos guia
        </h2>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        {values.map((value, index) => (
          <div class={`bg-white rounded-2xl p-6 shadow-sm hover:shadow-lg transition-shadow scroll-reveal reveal-delay-${index + 1}`}>
            <div class="w-12 h-12 bg-leaf-100 rounded-xl flex items-center justify-center mb-4">
              {value.icon === 'simple' && (
                <svg class="w-6 h-6 text-leaf-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
              )}
              {value.icon === 'access' && (
                <svg class="w-6 h-6 text-leaf-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                </svg>
              )}
              {value.icon === 'sustain' && (
                <svg class="w-6 h-6 text-leaf-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              )}
              {value.icon === 'community' && (
                <svg class="w-6 h-6 text-leaf-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                </svg>
              )}
            </div>
            <h3 class="font-bold text-gray-900 mb-2">{value.title}</h3>
            <p class="text-gray-600 text-sm">{value.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-16 bg-gradient-to-br from-leaf-600 via-leaf-700 to-leaf-800 relative overflow-hidden">
    <div class="absolute inset-0">
      <div class="absolute top-10 left-10 w-64 h-64 bg-white/5 rounded-full blur-3xl"></div>
      <div class="absolute bottom-10 right-10 w-96 h-96 bg-white/5 rounded-full blur-3xl"></div>
    </div>

    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center scroll-reveal">
      <div class="flex justify-center mb-6">
        <Mascote mood="excited" size="lg" />
      </div>
      <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">
        Pronto para comecar sua horta?
      </h2>
      <p class="text-xl text-leaf-100 mb-8 max-w-2xl mx-auto">
        Junte-se a mais de 2.500 pessoas que ja estao cultivando temperos frescos em casa.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/#quiz" class="inline-flex items-center justify-center px-8 py-4 bg-white text-leaf-700 rounded-xl font-bold text-lg hover:bg-leaf-50 hover:shadow-2xl transition-all group">
          Descobrir minha planta ideal
          <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>
        <a href="/produtos" class="inline-flex items-center justify-center px-8 py-4 bg-transparent text-white border-2 border-white/30 rounded-xl font-bold text-lg hover:bg-white/10 transition-all">
          Ver guias premium
        </a>
      </div>
    </div>
  </section>
</Layout>

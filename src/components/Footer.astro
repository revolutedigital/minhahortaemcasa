---
const currentYear = new Date().getFullYear();

const footerLinks = {
  guias: [
    { href: '/guias/horta-em-apartamento', label: 'Horta em Apartamento' },
    { href: '/guias/horta-vertical', label: 'Horta Vertical' },
    { href: '/guias/horta-na-varanda', label: 'Horta na Varanda' },
  ],
  plantas: [
    { href: '/plantas/manjericao', label: 'Manjericao' },
    { href: '/plantas/alecrim', label: 'Alecrim' },
    { href: '/plantas/hortela', label: 'Hortela' },
    { href: '/plantas/cebolinha', label: 'Cebolinha' },
    { href: '/plantas/salsinha', label: 'Salsinha' },
  ],
  recursos: [
    { href: '/problemas', label: 'Solucionar Problemas' },
    { href: '/produtos', label: 'Produtos Digitais' },
    { href: '/sobre', label: 'Sobre Nos' },
  ],
};
---

<footer class="bg-gray-900 text-gray-300">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <!-- Brand -->
      <div class="col-span-1">
        <a href="/" class="flex items-center gap-2 mb-4">
          <svg class="w-8 h-8 text-primary-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 2L12 6" stroke-linecap="round"/>
            <path d="M12 6C12 6 8 8 8 12C8 16 12 22 12 22C12 22 16 16 16 12C16 8 12 6 12 6Z" fill="currentColor" opacity="0.2"/>
            <path d="M12 6C12 6 8 8 8 12C8 16 12 22 12 22C12 22 16 16 16 12C16 8 12 6 12 6Z"/>
          </svg>
          <span class="font-bold text-xl text-white">
            Horta em Casa
          </span>
        </a>
        <p class="text-sm text-gray-400 mb-4">
          Aprenda a cultivar sua propria horta em casa, apartamento ou varanda. Guias praticos e dicas de cultivo.
        </p>
        <div id="newsletter" class="mt-4">
          <h4 class="text-sm font-semibold text-white mb-2">Receba dicas no seu email</h4>
          <form id="footer-newsletter-form" class="flex gap-2">
            <input
              type="email"
              name="email"
              placeholder="seu@email.com"
              required
              class="flex-1 px-3 py-2 bg-gray-800 border border-gray-700 rounded-lg text-sm text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent"
            />
            <button
              type="submit"
              class="px-4 py-2 bg-primary-600 text-white rounded-lg text-sm font-medium hover:bg-primary-700 transition-colors"
            >
              Enviar
            </button>
          </form>
        </div>
      </div>

      <!-- Guias -->
      <div>
        <h4 class="text-sm font-semibold text-white mb-4">Guias</h4>
        <ul class="space-y-2">
          {footerLinks.guias.map((link) => (
            <li>
              <a href={link.href} class="text-sm hover:text-primary-400 transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Plantas -->
      <div>
        <h4 class="text-sm font-semibold text-white mb-4">Plantas</h4>
        <ul class="space-y-2">
          {footerLinks.plantas.map((link) => (
            <li>
              <a href={link.href} class="text-sm hover:text-primary-400 transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Recursos -->
      <div>
        <h4 class="text-sm font-semibold text-white mb-4">Recursos</h4>
        <ul class="space-y-2">
          {footerLinks.recursos.map((link) => (
            <li>
              <a href={link.href} class="text-sm hover:text-primary-400 transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="border-t border-gray-800 mt-8 pt-8 flex flex-col sm:flex-row justify-between items-center gap-4">
      <p class="text-sm text-gray-500">
        &copy; {currentYear} Horta em Casa. Todos os direitos reservados.
      </p>
      <div class="flex gap-4">
        <a href="/privacidade" class="text-sm text-gray-500 hover:text-gray-400">Privacidade</a>
        <a href="/termos" class="text-sm text-gray-500 hover:text-gray-400">Termos de Uso</a>
      </div>
    </div>
  </div>
</footer>
